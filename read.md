# AniDesk v0.1.4 使用说明与更新记录

适用版本：Windows x64 版 AniDesk v0.1.4（Python / PySide6）。

说明编写日期：2026-09-06。本文依据当前 v0.1.4 源码核对，面向下载发行包的使用者。

> **v0.1.4 已修复悬浮窗无法通过鼠标唤出的问题。** 程序不再创建透明感应窗口，而是定时检查鼠标是否进入屏幕边缘坐标区域；托盘菜单同时增加“显示追更悬浮窗”作为手动入口。

## 1. 下载、运行与升级

1. 完整解压 `AniDesk-v0.1.4-win-x64.zip`。
2. 打开解压得到的 `AniDesk` 文件夹，双击 `AniDesk.exe`。
3. 保留与 exe 同目录的 `_internal` 文件夹，其中包含程序必需的运行库。无需自行安装 Python。
4. 首次启动会询问是否开机自启；之后可以在“设置”页调整。

分享时发送完整 ZIP。不能只复制一个 exe，也不要直接在压缩包预览界面中运行。

从旧版升级时，先通过托盘菜单“退出”，将新版完整解压到一个新目录后运行。同一 Windows 用户下，新旧版本默认访问同一份应用数据。若更换了程序路径并启用了开机自启，在新版中重新“保存设置”以更新启动路径。

点击主窗口右上角关闭按钮只会隐藏主界面，程序继续驻留系统托盘。彻底退出请右键托盘中的 AniDesk 图标，选择“退出”。

## 2. 本次 0.1.4 更新了什么

本次修复 0.1.3 完全透明感应区在 Windows 上可能发生鼠标穿透、导致悬浮窗无法唤出的问题。

| 原有问题 | v0.1.4 的调整 |
| --- | --- |
| 完全透明的感应窗可能收不到鼠标进入事件 | 移除感应窗，每约 80 毫秒直接检查鼠标是否进入停靠坐标区域 |
| 隐藏后没有可靠的手动显示方式 | 托盘菜单新增“显示追更悬浮窗” |
| 隐藏时依赖一个额外的透明顶层窗口 | 改用不绘制、不占用鼠标事件的虚拟感应区域 |

### 0.1.3 已包含的悬浮窗优化

| 原有问题 | 0.1.3 的调整 |
| --- | --- |
| 鼠标在不同屏幕操作时，悬浮窗跟着切换屏幕 | 按保存的屏幕名称定位；普通鼠标跨屏不会改变窗口归属 |
| 悬浮窗不能拖动 | 可按住顶部“近两日追更 · 拖动调整位置”文字区域拖动，松开后吸附到目标屏幕边缘 |
| 隐藏后还露出标签或边框 | 窗体完整移出屏幕后隐藏，改用透明边缘感应区唤出，并关闭系统投影 |
| 展开与隐藏切换突兀 | 加入约 180 毫秒的缓动动画 |
| 在相邻双屏的连接处隐藏可能露到另一块屏幕 | 检查隐藏区域与其他屏幕是否相交，优先选择不会露到邻屏的外侧边缘 |
| 重启后需要重新调整位置 | 手动拖动后保存屏幕、吸附方向及垂直位置 |
| 构建出的程序可能在加载 QtCore 时退出 | 打包配置统一使用 PySide6 配套 VC 运行库，并排除误从开发环境收集的冲突 ICU 文件 |

屏幕位置恢复依赖系统报告的屏幕名称；如果保存的屏幕当前不可用，程序回退到主屏。当前支持左、右贴边，拖动松开后会吸附，不是自由悬停模式。

### 此前版本已加入、0.1.4 继续包含的功能与修复

以下不是本次新增，但都已包含在 v0.1.4 中：

- 修复季度选择产生的 `'str' object has no attribute 'value'` 错误。
- Bangumi 季度目录按月份分页读取并去重，修复只读取部分结果导致的漏项。目录范围仍取决于接口返回的数据。
- 封面取消固定数量截断，按批次后台加载，每批完成后更新列表；下载失败或来源没有封面时仍可能无图。
- 悬浮看板列出本机日期“昨天”和“今天”的追更日程。
- 已看仓库支持在页面右侧直接编辑、保存看完日期和感想。

### 本次发布时的验证范围

v0.1.4 发布过程完成了 31 项自动化测试、真实 Windows 鼠标边缘触发检查，以及打包程序在隔离数据目录中的 10 秒启动检查，启动日志未记录 ERROR。多屏停靠沿用 0.1.3 已通过的实际双屏几何检查结果。

这些检查不代表已覆盖所有显示器排列、缩放比例、真实鼠标悬停和长时间运行场景，也不等同于 Windows 10/11 所有干净环境的完整验收。

## 3. 四个页面怎样使用

### 本季番剧

选择年份和季度，点击“刷新目录”。选中作品后点击“追更选中作品”，也可以双击作品添加追更。

标题等资料来自 Bangumi；封面在后台分批补齐。网络刷新失败时，如果该季度已有本地缓存，会继续显示缓存。

“本季”按所选季度内的首播月份查询，不等于“这个季度仍在更新的所有动画”。跨季度长篇作品可能需要回到其首播季度查找。

状态列目前直接显示英文，且采用首播日期估算：

| 显示值 | 含义 | 当前判定规则 |
| --- | --- | --- |
| `upcoming` | 尚未首播 | 首播日期晚于当前日期 |
| `airing` | 估计正在播出 | 距首播日期 0～150 天，包含第 150 天 |
| `finished` | 估计已完结 | 距首播日期超过 150 天 |
| `unknown` | 无法判断 | 没有首播日期 |

该列不是精确的官方完结状态，也不是你个人的观看进度。例如长篇作品播出超过 150 天后仍可能被估算为 `finished`。估算在读取远端条目时执行，离线缓存可能保留旧结果。

### 我的追更

- “同步日程”：为选中的作品读取 AniList 集数及播出时间。
- “选择匹配”：自动匹配不成功或作品匹配错误时，手动选择候选。
- “手动时间”：覆盖自动日程；留空后恢复使用自动日程。
- “提醒设置”：设置该作品是否提醒及提前分钟数；作品级提前量填 0 表示使用全局设置。
- “添加地址”：为作品保存 HTTP/HTTPS 播放地址；支持默认项、上移和删除。
- “打开地址”：使用系统默认处理程序打开保存的地址。
- “移入仓库”：填写看完日期和感想，将作品转入“已看仓库”。

手动时间输入框目前要求 UTC ISO 8601 格式。例如北京时间 2026-09-06 20:00，应输入 `2026-09-06T12:00:00Z`。页面展示时会转换到本机时区。手动时间是一次具体日期时间，不会自动每周重复。

### 已看仓库

选中仓库中的作品，在右侧“仓库记录编辑”区域修改看完日期与感想，然后点击“保存日期和感想”。编辑后应先保存，再切换作品或离开页面；当前不是自动保存。

也可以通过“搜索 Bangumi”添加过去的作品，或用“手动添加”填写作品信息及本地封面。需要重新追更时，选中记录后点击“恢复追更”。

### 设置

可调整全局提前分钟、日程刷新小时、系统通知、置顶悬浮提醒及开机自启；修改后点击“保存设置”。

默认提前 15 分钟提醒，默认每 6 小时同步日程。系统通知开关与悬浮窗开关各自独立。

此页还提供“导出追更备份”“导出仓库备份”和“从 .anibackup 恢复”。

## 4. 提醒窗现在如何显示

这里包含两种行为：屏幕边缘的“近两日追更”看板，以及到时间触发的 Windows 系统通知。两者使用不同的筛选条件。

### 看板列出哪些作品

看板从本地保存的追更记录和日程读取内容，按本机时区选取“昨天”和“今天”这两个自然日：

- 昨天已播出的集数。
- 今天已经播出的集数。
- 今天稍后才播出的集数。
- 设置了手动时间时，该作品以手动时间为准，暂不使用其自动日程。
- 关闭单作品提醒、已提醒过或延后提醒，不会因此从近两日看板中排除作品。
- 未追更的作品、仅在已看仓库中的作品、明天及更远日期的日程不在看板中。

看板显示的是“已经获取的日程”，不是所有追更作品的固定名单。某作品尚未匹配到 AniList、没有成功获取日程，或日期不属于昨天/今天，就没有对应条目。当前同步会用接口返回的日程替换旧日程，因此也不能保证所有昨日集数始终都有记录。

界面中的“已更新”只表示已到计划播出时间，没有检查你保存的视频网站是否已经上线该集。

### 展开与隐藏规则

| 场景 | 当前代码行为 |
| --- | --- |
| 程序启动，悬浮提醒已开启 | 默认处于收起状态，保留边缘感应区 |
| 鼠标进入对应边缘感应区 | 由约 80 毫秒一次的位置检查触发展开，动画约 180 毫秒 |
| 在托盘选择“显示追更悬浮窗” | 立即请求展开看板；关闭悬浮提醒时该菜单项不可用 |
| 鼠标离开看板 | 短暂延迟后收起；初始延迟约 0.5 秒，拖动后定时器会调整为约 1.2 秒 |
| 鼠标正在看板内、拖动中或地址下拉菜单仍打开 | 暂缓自动收起 |
| 点击“贴边隐藏” | 收起看板，不取消追更，也不清空列表 |
| 到了提醒时间 | 可发送 Windows 系统通知，并刷新看板内容；不会强制展开已收起的看板 |
| 没有任何追更作品 | 感应区仍保留；展开后显示“昨天和今天暂无已获取的更新日程” |
| 有追更，但近两日没有有效日程 | 同样显示空状态，不会仅因列表为空而关闭感应区 |
| 在设置中关闭悬浮提醒并保存 | 看板和感应区都隐藏，鼠标碰边缘也不会展开 |
| 从托盘彻底退出程序 | 看板、感应区和定时检查全部结束 |

“常驻”是程序及感应区持续存在，当前没有“始终展开”的开关。

### 感应区在哪里

感应区宽约 4 个 Qt 逻辑像素，高约 260 个逻辑像素，只覆盖看板对应的垂直范围，不是整条屏幕边缘；显示缩放会影响实际像素尺寸。

未保存过位置时，默认位于主屏可用区域的右侧，上端距顶部约 24 个逻辑像素；若该侧会露到邻屏，则优先切换到可用的另一侧。

拖动之后，感应区跟随保存的屏幕、左/右方向和高度。因此隐藏后应去之前停靠的边缘位置触碰，不是鼠标当前所在屏幕任意位置。

### 为什么看不到提醒窗

先在“设置”中确认已勾选“启用置顶悬浮提醒”，点击“保存设置”，然后将鼠标移到上次停靠位置的屏幕边缘。

如果从未拖动过，可以先在主屏右上方边缘、约顶部 24～284 个逻辑像素范围内寻找；右侧与其他屏幕相邻时，也检查主屏左侧对应范围。

如果展开后显示空状态，检查“我的追更”中是否有作品、匹配是否正确，以及是否已经取得昨天/今天的日程。新追更或日程修改后，列表可能需要等下一次检查刷新。

如果边缘暂时无法唤出窗口，可右键托盘中的 AniDesk 图标，选择“显示追更悬浮窗”。空列表不会禁用悬浮窗；展开后会显示近两日暂无日程。

### 到点通知规则

程序通常每 60 秒检查一次提醒并刷新看板；启动后约 2 秒安排一次检查，全量日程同步完成、保存设置及应用重新变为活动状态时也会触发检查。日程同步默认每 6 小时一次，与每分钟提醒检查不同。

到点系统通知要求开启系统通知及该作品的提醒。使用作品级提前量或全局提前量，手动时间优先；在“计划时间减提前量”到“计划时间后两小时”之间判断是否提醒，并通过已提醒标记避免同一日程重复提醒。

“延后提醒”默认延后 10 分钟，同时收起看板；到期后仍需满足提醒时间窗口。已经超出两小时窗口的昨日集数可以继续出现在看板上，但不会因此重新触发到点通知。

程序需要持续运行才能检查提醒；彻底退出或电脑休眠期间无法执行定时检查。当前代码在应用恢复活动状态时补做检查，并非专用的 Windows 休眠恢复事件监听。

## 5. 数据在哪里、怎样备份

默认数据位置为：

```text
%LOCALAPPDATA%\AniDesk
```

按 Win + R，输入该路径即可打开。普通 Windows 用户通常对应 `C:\Users\你的用户名\AppData\Local\AniDesk`。

| 文件或目录 | 用途 |
| --- | --- |
| `anidesk.db` | 作品资料、追更、日程、播放地址、仓库日期与感想、应用设置 |
| `covers\` | 封面缓存 |
| `backups\` | 数据迁移或导入前的数据库快照 |
| `logs\` | 运行日志 |
| `anidesk.db-wal`、`anidesk.db-shm` | SQLite 运行时可能产生的辅助文件 |

数据位置由代码使用 Windows 的 LOCALAPPDATA 目录决定，并未写死 C 盘盘符。v0.1.4 没有应用内自定义存储目录的选项，也不会随 exe 的位置移动数据目录。

悬浮窗位置单独通过 QSettings 保存，在 Windows 上通常位于当前用户注册表 `HKEY_CURRENT_USER\Software\AniDesk\AniDesk`。不要将 .anibackup 视为所有 Windows 配置的完整克隆。

迁移追更和已看仓库时，在“设置”中分别导出两类 .anibackup，到新环境分别恢复。导出的文件保存到你在对话框选择的位置，不一定在 backups 目录。导入前会创建数据库快照，重复记录按现有规则合并。

发送发行 ZIP 不包含你保存在用户数据目录中的个人追番记录；需要分享数据时应另行导出备份。

## 6. 当前边界

- 面向 Windows 10/11 x64，发行形式为完整目录 ZIP。
- Bangumi 提供目录资料，AniList 提供日程，获取新数据和新封面需要网络。
- 无账号同步、自动更新或云同步。
- 不提供或抓取播放资源，也不验证视频网站每一集的实际上线状态。
- 当前状态列是首播日期估算值；感想需点击保存；悬浮窗默认自动收起。
