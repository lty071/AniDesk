# AniDesk v0.1.8 使用说明

适用版本：Windows 10/11 x64，Python / PySide6 主版本 v0.1.8。当前文件名为 `AniDesk-v0.1.8.exe`，下文的 exe 均指实际运行文件。

## 1. 运行、升级与迁移

将 `AniDesk.exe` 放到有写入权限的文件夹，双击运行，无需安装 Python。当前发行形式为单文件便携 exe，无需安装包或配套 `_internal` 文件夹。程序启动时会临时解压运行库，首次启动可能需要稍等。

个人数据自动保存在 exe 同目录的 `data/` 中，程序目录例如：

```text
AniDesk/
  AniDesk.exe
  data/
    anidesk.db
    covers/
    backups/
    logs/
    settings.ini
    appearance.json
    themes/backgrounds/
```

升级前请右键托盘图标选择“退出”，再用新版 exe 替换原文件，保留 `data/`。如果移动目录或换电脑，请一起复制 **exe 和整个 `data/` 文件夹**。只复制 exe 会得到独立的新环境，不会随附原电脑的个人追番记录。

第一次启动且 `data/anidesk.db` 不存在时，会优先从旧 Python 版的 `%LOCALAPPDATA%/AniDesk` 复制数据；没有旧 Python 数据库时，再尝试旧 Tauri 版数据。数据库通过 SQLite 快照复制，并迁入已有封面和备份，旧数据保持原样。已有便携版数据库不会被旧数据覆盖。

首次启动会询问是否开机自启，之后可在“设置”页调整。移动 exe 后，如果需要继续自启，请在新位置启动程序并重新“保存设置”以更新启动路径。

关闭主窗口只会隐藏界面，程序继续驻留系统托盘；彻底结束请在托盘菜单选择“退出”。

## 2. v0.1.8 的变化

本季番剧采用海报浏览，点击卡片查看详情，点击卡片底部“追更”添加；已看作品按看完月份整理为收藏册。

仓库中的“阅读全文 / 编辑感想”打开独立写作页，默认最大化，支持长篇纯文本。F11 切换全屏，Ctrl+S 或顶部“保存感想”保存；关闭未保存的文章时可选择保存、放弃或取消。感想没有 500 字短评限制，仓库备份包含全文。保存失败时正文仍留在编辑器中，不要强制结束程序。

通用设置分为提醒、同步与启动、备份卡片；外观设置提供主题缩略图、效果预览和侧栏图片预览。小窗口中设置卡片纵向排列，可滚动查看。

悬浮窗使用 A 方案竖向日程卡，今天在前、昨天在后；卡片显示封面、时间与集数，底部可选择地址、打开、延后 10 分钟及贴边隐藏。

### 延续的周历与插画侧栏


“我的追更”默认显示周一到周日的放送日历。点击卡片可在右侧查看详情、打开地址、匹配日程或调整手动时间。上方可以切换周次、返回本周或同步日程；“全部追更”中仍可找到所有作品，包括没有有效日程的作品。小窗口默认收起详情，可点击“作品详情”展开，日历可横向滚动。

周历只显示本地已获取或手动设置的确切时间，不会仅凭首播星期推测后续更新。切换周次不会额外查询历史日程。“时间已到”只说明已到计划播出时间，不代表播放网站已上线。

左侧支持内置插画与自己的图片。入口统一在 **设置 → 外观 → 侧栏图片**，图片下方没有换图按钮。可选择“系统内置”“我的图片”或“不显示图片”，添加 PNG/JPG/WebP 图片，调整位置、缩放和顶部渐隐，再点击“应用外观”保存。“恢复默认侧栏”仅重置侧栏显示，完整“恢复默认”会重置所有外观选项。

内置的海风来信、静夜星河和樱花小径已随 exe 打包，断网也能切换。自己的图片会保存到数据目录中的背景副本目录，移动原图不影响使用；侧栏图片和主窗口背景各自独立。

EXE 文件名带版本号。升级前从托盘退出旧版，将 `AniDesk-v0.1.8.exe` 放在原 `data/` 的同级目录启动，即可沿用数据。若启用开机自启，请在新版中重新保存通用设置，更新启动文件路径。

### 保留的主题设置

新增“设置 → 外观”，提供三套主题：浅色的“樱花日常”、深色的“星空放映室”和清爽的“夏日汽水”。主窗口、应用内弹窗和悬浮窗统一换肤，系统文件选择窗口仍使用 Windows 外观。

可以选择强调色、调整字号和主题装饰，导入自己的背景图片。支持 PNG/JPG/WebP，最大 30 MB、4000 万像素；导入时自动保存适合界面显示的本地副本。背景居中铺满，超出窗口比例的部分会裁切。可通过亮度、柔化程度和面板不透明度改善可读性。

修改即时预览，点击“应用外观”才保存；“撤销预览”回到上次保存状态，“恢复默认”先预览默认主题，仍需点击应用才会保存。退出程序时未保存的预览不会保留。切换预设会采用该主题的强调色，已选背景和字号继续保留。

以下是继续保留的 v0.1.5 行为：

| 修改 | 当前行为 |
| --- | --- |
| 数据随程序保存 | 数据库、封面、备份、日志和悬浮窗位置都保存在程序旁的 `data/` 中 |
| 修正下一集时间 | 显示未来有效日程中最早一集的播出时间，不再把已播出的第一集当作下一集 |
| 取消提前提醒 | 移除提前分钟及单作品“提醒设置”，系统通知在播出时间到达后检查发送 |
| 显示每周播出星期 | 本季番剧和我的追更显示星期；实际日程优先，首播日期推算会明确标记，没有信息则显示“未获取” |
| 单文件发行 | 直接运行 `AniDesk.exe`，无需安装或附带运行库目录 |

旧备份中的提前分钟和单作品提醒开关字段仅为格式兼容保留，不再参与提醒判断。系统通知、悬浮窗开关、手动时间和延后提醒继续保留。

## 3. 四个页面怎样使用

### 本季番剧

选择年份和季度，点击“刷新目录”。选中作品后点击“追更选中作品”，也可以双击作品添加追更。

标题等资料来自 Bangumi，封面在后台分批补齐；网络失败时使用已有季度缓存。“本季”按所选季度内的首播月份查询，不等于“这个季度仍在更新的所有动画”，跨季度长篇可能需要回到首播季度查找。

状态列采用首播日期估算，不能视为官方完结状态：

| 显示值 | 含义 | 判定规则 |
| --- | --- | --- |
| `upcoming` | 尚未首播 | 首播日期晚于当前日期 |
| `airing` | 估计正在播出 | 距首播日期 0～150 天 |
| `finished` | 估计已完结 | 距首播日期超过 150 天 |
| `unknown` | 无法判断 | 没有首播日期 |

状态后的星期列优先根据已获取的播出日程显示。只有首播日期时，会明确标记为推算，不能保证后续每集都在同一天播出；没有可用资料时显示“未获取”。尚未匹配或同步 AniList 的作品通常只能使用首播日期推算。

### 我的追更

- “同步日程”：为选中的作品读取 AniList 集数及播出时间。
- “选择匹配”：自动匹配失败或作品匹配错误时，手动选择候选。
- “手动时间”：覆盖自动日程；留空并保存后恢复自动日程。
- “添加地址”：保存 HTTP/HTTPS 播放地址，支持默认项、上移和删除。
- “打开地址”：使用系统默认处理程序打开保存的地址。
- “移入仓库”：填写看完日期，将作品转入“已看仓库”，随后可打开独立页面撰写完整感想。

“下一集”显示当前时间之后最早的有效日程。没有已获取的未来日程时，不会用过去的第一集填充，需要尝试同步日程、检查匹配或设置手动时间。星期依据实际日程显示；只能从首播日期判断时会标记推算，资料不足时显示“未获取”。页面时间与星期按本机时区显示。

手动时间是一次具体日期时间，不会自动每周重复。它优先于自动日程；如果手动时间已过，该作品不会因此回退到自动日程显示下一集，需要修改或清除手动时间。

手动时间输入使用 UTC ISO 8601 格式。例如北京时间 2026-09-07 20:00，输入 `2026-09-07T12:00:00Z`。页面显示时会转换到本机时区。

### 已看仓库

选中作品，在右侧编辑看完日期并点击“保存日期”。点击“写长篇感想”或“阅读全文 / 编辑感想”进入独立写作页，使用顶部保存按钮或 Ctrl+S 保存全文。

可以通过“搜索添加”查找 Bangumi 作品，或用“手动添加”填写作品信息及本地封面。地址管理、恢复追更和删除记录位于右侧“更多”菜单；恢复追更会移除仓库日期与感想，操作前会提示确认。

### 设置

可调整日程刷新小时、系统通知、置顶悬浮提醒和开机自启，修改后点击“保存设置”。默认每 6 小时同步日程，系统通知开关和悬浮窗开关相互独立。

此页还提供“导出追更备份”“导出仓库备份”和“从 .anibackup 恢复”。不再提供提前分钟设置。

## 4. 看板与到点通知

### 近两日追更看板

悬浮看板按本机日期列出昨天和今天已获取的追更日程，包括今天稍后才播出的集数。设置手动时间时，以手动时间为准，暂不使用自动日程。

看板不会因为某日程已提醒过或延后提醒就移除该条目。未追更、只在已看仓库中、没有有效日程或日程位于其他日期的作品不会出现。同步会用接口返回的日程更新本地内容，因此不保证所有昨日集数始终保留。

“计划时间已到”只表示已到计划播出时间，没有检查播放网站是否已经上线该集。

启用悬浮窗后，程序默认贴边收起。鼠标移到上次停靠位置的屏幕边缘即可展开，也可以在托盘菜单选择“显示追更悬浮窗”。按住看板顶部拖动，可将其固定到另一屏幕的左边或右边；普通鼠标跨屏不会改变看板位置。

鼠标离开后看板会短暂延迟再收起，拖动或地址菜单展开期间暂缓隐藏。点击“贴边隐藏”不会取消追更或清空列表。到了通知时间只会刷新内容，不会强制展开已收起的看板。

边缘感应区只覆盖看板对应的垂直范围，不是整条屏幕边缘。未保存位置时通常位于主屏右上侧；右侧与其他屏幕相邻时，可能选择另一侧。找不到位置时，优先使用托盘菜单手动展开，再拖动调整。

空列表仍可以展开，会显示近两日暂无已获取日程。关闭“启用置顶悬浮提醒”并保存后，看板和边缘感应都会停用。保存的屏幕当前不可用时，位置会回退到主屏。

### 系统通知

程序通常每 60 秒检查提醒并刷新看板；启动、全量日程同步完成、保存设置及应用重新变为活动状态时，也会安排检查。

开启系统通知后，程序从计划播出时间开始，在计划时间后两小时的窗口内判断是否提醒，并使用已提醒标记避免同一日程重复通知。手动时间仍优先，不使用任何提前分钟。检查采用定时轮询，因此通知可能晚于计划时间几十秒。

“延后提醒”默认延后 10 分钟，同时收起看板；延后到期后仍需处在提醒窗口内。已经超出窗口的昨日集数可以显示在看板上，但不会因此重新触发系统通知。

程序必须运行才能检查通知。彻底退出或电脑休眠期间无法检查；重新启动或恢复活动后，仅仍符合提醒窗口的日程可能补发。

## 5. 数据与备份

| 文件或目录 | 用途 |
| --- | --- |
| `data/anidesk.db` | 作品、追更、日程、播放地址、仓库记录和应用设置 |
| `data/covers/` | 封面缓存 |
| `data/backups/` | 数据迁移或导入前的数据库快照 |
| `data/logs/` | 运行日志 |
| `data/settings.ini` | 悬浮窗屏幕、贴边方向与位置 |
| `data/appearance.json` | 已保存的主题、字号、强调色与背景显示配置 |
| `data/themes/backgrounds/` | 导入背景的本地副本，移动原图片不影响使用 |
| `data/anidesk.db-wal`、`data/anidesk.db-shm` | SQLite 运行时可能产生的辅助文件 |

数据目录以实际 exe 所在位置为准，不受快捷方式的“起始位置”影响，也不会写入单文件运行库的临时解压目录。源码开发运行的数据固定在 `anidesk_py/data/`，与 `run_anidesk.py` 相邻。

复制完整数据前先通过托盘退出程序，再复制整个 `data/`。迁移追更和已看仓库也可以在“设置”中分别导出 `.anibackup`，到新环境分别恢复；导入前会创建数据库快照，重复记录按现有规则合并。导出位置由保存对话框决定，不一定在 `data/backups/` 中。

`.anibackup` 用于记录迁移，不是全部程序配置的完整克隆；包括悬浮窗位置、外观设置与背景在内的完整迁移应复制整个 `data/`。发送 exe 不包含个人记录，分享时附带 `data/` 才会包含其中的数据。

## 6. 当前边界

- 获取 Bangumi 资料、AniList 日程及新封面需要网络；没有成功获取的日程无法保证下一集时间与播出星期准确。
- 首播日期推算的星期和状态可能不符合停播、改期或长篇作品的实际情况。
- 无账号同步、自动更新或云同步，不提供或抓取播放资源。
- v0.1.8 指 Python 主版本；仓库中的 Tauri v0.1.0 仅作为旧版兼容参考。
